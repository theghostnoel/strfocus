rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Relaxed rules to ensure seamless operation with or without Firebase Auth enabled,
    // and to support custom db-backed fallback authentication.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
